import SkeletonExplorer from '@/components/SkeletonExplorer';

export const metadata = {
  title: 'Skeleton (rig POC) · The Atlas',
};

export default function SkeletonPage() {
  return <SkeletonExplorer />;
}
